import {chromium} from 'playwright';
import {readFile,mkdir} from 'node:fs/promises';
import path from 'node:path';
import assert from 'node:assert/strict';
const base=path.resolve(import.meta.dirname,'..'),host='https://muniverse-official.github.io',origin='https://kkaoerbblpuszptiibvo.supabase.co';
await mkdir(path.join(base,'qa-screenshots'),{recursive:true});
const browser=await chromium.launch({headless:true});
const context=await browser.newContext();
const errors=[];context.on('page',page=>page.on('pageerror',e=>errors.push(e.message)));
await context.route(host+'/music-core-attendee/**',async route=>{const u=new URL(route.request().url());let relative=decodeURIComponent(u.pathname.replace('/music-core-attendee/',''));if(!relative||relative.endsWith('/'))relative+='index.html';const file=path.resolve(base,'site',relative);if(!file.startsWith(path.join(base,'site')+path.sep))return route.abort();try{const body=await readFile(file);const ext=path.extname(file);return route.fulfill({body,contentType:({'.html':'text/html','.js':'text/javascript','.css':'text/css','.svg':'image/svg+xml','.png':'image/png','.webp':'image/webp'})[ext]||'text/plain'});}catch{return route.fulfill({status:404})}});
let selection='reserve',registered=false;const submissions=[];
const config={ok:true,program:'music_core',publishedAt:new Date().toISOString(),openAt:new Date(Date.now()-3600000).toISOString(),closeAt:new Date(Date.now()+3600000).toISOString(),paused:false,testMode:false,eventDateTba:false,eventDate:'2026-09-19',showEventDate:false};
await context.route(origin+'/**',async route=>{let result;
const u=new URL(route.request().url());
if(u.pathname.includes('/storage/'))result=config;
else if(u.searchParams.get('action')==='clock')result={ok:true,serverTime:new Date().toISOString()};
else if(u.searchParams.get('action')==='submit'){submissions.push(route.request().postDataJSON());registered=true;result={ok:true,registered:true,selectionType:selection,deliveryQueued:selection==='primary',eventDate:'2026-09-19'};}
else result=registered?{ok:false,code:'ALREADY_SUBMITTED',selectionType:selection}:{ok:true,selectionType:selection,token:'mock-only',eventDate:'2026-09-19'};
return route.fulfill({status:result.ok?200:404,contentType:'application/json',headers:{'access-control-allow-origin':host},body:JSON.stringify(result)});
});
try{
const page=await context.newPage();await page.setViewportSize({width:390,height:844});await page.goto(host+'/music-core-attendee/');await page.waitForFunction(()=>document.documentElement.dataset.availability==='open');
await page.selectOption('#lang','ko');await page.fill('#email','reserve@example.invalid');await page.fill('#nickname','예비 테스트');await page.locator('label[for="consent"]').click();await page.click('#verifyBtn');await page.locator('#reserve').waitFor({state:'visible'});
assert.equal(await page.locator('#reserveTitle').textContent(),'예비 당첨자로 선정되셨습니다.');assert.match(await page.locator('#reserveBody').textContent(),/개별 연락/);assert.equal(await page.locator('#step2').isVisible(),true);assert.equal(await page.locator('#reserveBack').count(),0);
const titleCopy={ko:'예비 당첨자로 선정되셨습니다.',en:'You have been selected as a reserve winner.',ja:'補欠当選者に選ばれました。','zh-TW':'您已入選候補名單。','zh-CN':'您已入选候补名单。'};
const doneCopy={ko:'예비 당첨자 정보 등록 완료',en:'Reserve winner information registered',ja:'補欠当選者情報の登録が完了しました','zh-TW':'候補中獎者資料登記完成','zh-CN':'候补中奖者信息登记完成'};
const caveat={ko:/확정되는 것은 아닙니다/,en:/does not confirm attendance/,ja:/観覧は確定しません/,'zh-TW':/不代表已確定/,'zh-CN':/不代表已确定/};
const fields=['#name','#birthDate','#koreanPhone','#xAccount','#contactEmail'];
for(const lang of ['en','ja','zh-TW','zh-CN','ko']){await page.selectOption('#lang',lang);assert.equal(await page.locator('#reserveTitle').textContent(),titleCopy[lang]);assert.ok((await page.locator('#reserveInstruction').textContent()).length>15);assert.equal(await page.locator('#reserve').isVisible(),true);for(const id of fields)assert.equal(await page.locator(id).isVisible(),true,id+' same field');}
assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);
await page.screenshot({path:path.join(base,'qa-screenshots/reserve-mobile.png'),fullPage:true});
await page.click('#submitBtn');assert.equal(submissions.length,0);assert.ok((await page.locator('#submitMessage').textContent()).length>0);
await page.fill('#name','Reserve QA');await page.fill('#birthDate','2000-01-01');await page.locator('label').filter({has:page.locator('input[name="nationalityMode"][value="NON_KR"]')}).click();await page.selectOption('#country','JP');await page.locator('label').filter({has:page.locator('input[name="phoneMode"][value="INTL"]')}).click();await page.fill('#internationalPhone','+81 90 1234 5678');await page.fill('#xAccount','@reserve_qa');await page.click('#submitBtn');await page.locator('#done').waitFor({state:'visible'});
assert.equal(submissions.length,1);assert.equal(submissions[0].name,'Reserve QA');assert.equal(submissions[0].nationality,'JP');assert.equal(submissions[0].birth_date,'2000-01-01');assert.equal(submissions[0].phone,'+819012345678');assert.equal(submissions[0].x_account,'@reserve_qa');assert.equal(submissions[0].contact_email,'reserve@example.invalid');assert.equal(await page.locator('#step2').isVisible(),false);assert.equal(await page.locator('#reserve').isVisible(),false);
for(const lang of ['en','ja','zh-TW','zh-CN','ko']){await page.selectOption('#lang',lang);assert.equal(await page.locator('#doneTitle').textContent(),doneCopy[lang]);assert.match(await page.locator('#doneSub').textContent(),caveat[lang]);}
await page.screenshot({path:path.join(base,'qa-screenshots/reserve-registered-mobile.png'),fullPage:true});
async function lookup(email,nickname){await page.reload();await page.waitForFunction(()=>document.documentElement.dataset.availability==='open');await page.selectOption('#lang','ko');await page.fill('#email',email);await page.fill('#nickname',nickname);await page.locator('label[for="consent"]').click();await page.click('#verifyBtn');}
await lookup('reserve@example.invalid','예비 테스트');await page.locator('#already').waitFor({state:'visible'});
for(const lang of ['en','ja','zh-TW','zh-CN','ko']){await page.selectOption('#lang',lang);await page.waitForFunction(()=>document.querySelector('#alreadyMessage').textContent.length>0);assert.match(await page.locator('#alreadyReserveNote').textContent(),caveat[lang]);assert.match(await page.locator('#alreadyMessage').textContent(),/예비|reserve|補欠|候補|候补/);}
selection='primary';registered=false;await lookup('primary@example.invalid','본 테스트');await page.locator('#step2').waitFor({state:'visible'});assert.equal(await page.locator('#reserve').isVisible(),false);for(const id of fields)assert.equal(await page.locator(id).isVisible(),true);
await page.fill('#name','Primary QA');await page.fill('#birthDate','2000-01-01');await page.fill('#koreanPhone','01012345678');await page.fill('#xAccount','@primary_qa');await page.click('#submitBtn');await page.locator('#done').waitFor({state:'visible'});assert.equal(submissions.length,2);assert.doesNotMatch(await page.locator('#doneTitle').textContent(),/예비/);
const rid='11111111-1111-4111-8111-111111111111';const cfg={event_date:'2026-09-19',event_date_tba:'false',registration_open_at:config.openAt,registration_close_at:config.closeAt,show_event_date:'false',test_mode:'false',registration_paused:'false'};
const round={id:rid,title:'964회차',program:'music_core',is_public:true,archived:false,version:1,config:cfg};
const payload={ok:true,username:'qa',expiresAt:new Date(Date.now()+7200000).toISOString(),serverTime:new Date().toISOString(),history:[],music_core:{active_round_id:rid,rounds:[round],winners:[{id:'22222222-2222-4222-8222-222222222222',round_id:rid,email:'main@example.invalid',nickname:'본 당첨 QA',selection_type:'primary',submitted:false},{id:'33333333-3333-4333-8333-333333333333',round_id:rid,email:'reserve@example.invalid',nickname:'예비 QA',selection_type:'reserve',submitted:true,name:'Reserve QA',birth_date:'2000-01-01',nationality:'JP',phone:'+819012345678',x_account:'@reserve_qa',contact_email:'reserve@example.invalid'}]},fans_pick:{rounds:[{...round,program:'fans_pick',title:'1회차'}],winners:[],active_round_id:rid}};
const writes=[];
await context.route(origin+'/functions/v1/attendee-admin',async route=>{const b=route.request().postDataJSON();let result={ok:true};if(b.action==='login')result={ok:true,token:'a'.repeat(64)};else if(b.action==='get')result=payload;else{writes.push(b);if(b.action==='promote_reserve')payload.music_core.winners.find(w=>w.id===b.id).selection_type='primary';if(b.action==='add_winners'){result={ok:true,added:b.winners.length,duplicates:0};payload.music_core.winners.push(...b.winners.map((w,i)=>({...w,id:'added-'+i,round_id:rid,selection_type:b.selection_type,submitted:false})))}}return route.fulfill({status:200,contentType:'application/json',headers:{'access-control-allow-origin':host},body:JSON.stringify(result)})});
const admin=await context.newPage();await admin.setViewportSize({width:1400,height:1000});await admin.goto(host+'/music-core-attendee/admin/');await admin.fill('#username','qa');await admin.fill('#password','test-only');await admin.click('#loginButton');await admin.locator('#dashboard').waitFor({state:'visible'});await admin.waitForFunction(()=>document.body.getAttribute('aria-busy')==='false');await admin.click('#winnersTab');
assert.equal(await admin.locator('#winnerRows tr').count(),1);assert.match(await admin.locator('#winnerRows').textContent(),/본 당첨 QA/);assert.equal(await admin.locator('#reserveCount').textContent(),'1');
await admin.click('[data-selection="reserve"]');assert.equal(await admin.locator('#winnerRows tr').count(),1);assert.match(await admin.locator('#winnerRows').textContent(),/예비 QA/);assert.equal(await admin.locator('#statusFilter').isVisible(),true);assert.match(await admin.locator('#winnerRows').textContent(),/등록 완료/);
await admin.fill('#winnerInput','reserve2@example.invalid, 추가 예비 QA');await admin.click('#addButton');await admin.waitForFunction(()=>document.body.getAttribute('aria-busy')==='false');assert.equal(writes.at(-1).selection_type,'reserve');assert.equal(await admin.locator('#winnerRows tr').count(),2);
await admin.selectOption('#statusFilter','registered');assert.equal(await admin.locator('#winnerRows tr').count(),1);
const downloaded=admin.waitForEvent('download');await admin.click('#exportCsv');const csv=await readFile(await (await downloaded).path(),'utf8');assert.match(csv,/생년월일/);assert.match(csv,/2000-01-01/);assert.match(csv,/819012345678/);assert.match(csv,/Reserve QA/);await admin.selectOption('#statusFilter','all');
await admin.screenshot({path:path.join(base,'qa-screenshots/reserve-admin.png'),fullPage:true});
await admin.locator('#winnerRows tr').first().getByRole('button',{name:'본 당첨자로 전환'}).click();await admin.click('#confirmAccept');await admin.waitForFunction(()=>document.body.getAttribute('aria-busy')==='false');assert.equal(writes.at(-1).action,'promote_reserve');assert.equal(await admin.locator('#winnerRows tr').count(),1);await admin.click('[data-selection="primary"]');assert.equal(await admin.locator('#winnerRows tr').count(),2);
await admin.click('[data-program="fans_pick"]');assert.equal(await admin.locator('#selectionTabs').isVisible(),false);assert.equal(await admin.locator('#reserveStat').isVisible(),false);assert.deepEqual(errors,[]);
console.log('PASS reserve same fields + foreign phone registration, all five languages through completion and repeated lookup, primary registration, separate lists, registration filter and CSV, promotion, FANS PICK compatibility');
}catch(error){for(const [index,page] of context.pages().entries())await page.screenshot({path:path.join(base,'qa-screenshots/reserve-failure-'+index+'.png'),fullPage:true}).catch(()=>{});console.error('PAGE ERRORS',errors);throw error}finally{await browser.close()}
